name: 'Build JASP desktop'
description: 'Auto build JASP application on Win/Mac/Linux'

jobs:
      
   build-jasp:
   
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.os }} 

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macOS-latest, windows-latest]
        
    steps:
      - uses: actions/checkout@v3

      - name: install Qt6
        uses: jurplel/install-qt-action@v3
        with:
           version: 6.4.*
